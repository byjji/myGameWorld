# Game-Party — 스마트폰 동작인식 파티게임

조카(2021년생, 만 5세)와 함께 놀기 위한 웹 기반 동작인식 미니게임 모음.
폰을 컨트롤러로 쓰고 TV 화면에서 플레이하는 마리오파티 방식.

프로젝트명 `Game-Party`는 임시. 코드 네임스페이스는 `GP`를 쓴다.
주인공 캐릭터의 모자 마크는 여전히 `L`이다 — 이름과 무관한 캐릭터 디자인이다 (3장).

---

## 1. 핵심 구조

```
조카 집                              우리 집
┌─────────────┐                   ┌──────────────┐
│ 폰 (컨트롤러) │ ──센서값──┐        │  NAS         │
│  /play      │           ├─WSS─→ │  FastAPI     │
├─────────────┤           │        │  릴레이 서버   │
│ TV (화면)    │ ←─게임상태─┘        └──────────────┘
│  /tv        │
└─────────────┘                   Netlify: 정적 파일 (HTML/JS/이미지/사운드)
```

- **폰은 컨트롤러 전용.** 게임 화면을 절대 그리지 않는다. 그리면 조카가 TV 대신 폰을 본다.
- **TV는 화면 전용.** 입력을 받지 않는다.
- **정적 파일은 Netlify, WebSocket 릴레이만 NAS.** 우리 집 업로드 대역으로 에셋을 매번 내보내지 않기 위함.
- 예상 왕복 지연 30~50ms (국내망 기준). 게임 성립 범위.

### 오프라인 모드는 만들지 않는다

의도적 결정. 폰만으로 돌아가면 조카가 혼자 계속 하게 되어 "같이 하는 놀이"가 아니라 그냥 게임 앱이 된다.
TV 연결이 필요하다는 마찰이 어른의 개입을 강제하는 장치로 작동한다.

---

## 2. 기술 스택

| 영역 | 선택 |
|---|---|
| 프론트엔드 | 바닐라 JS (SPA 프레임워크 없음), 2D Canvas |
| 백엔드 | FastAPI (정적 서빙 + WebSocket 릴레이만) |
| 배포 | Netlify(정적) + Synology NAS Docker(릴레이) |
| DB | **없음.** 센서값은 받아서 즉시 넘기고 버린다 |
| 상태 저장 | 서버 메모리 딕셔너리 (재시작 시 방 소멸, 재접속하면 그만) |

### 렌더링 제약 — TV 내장 브라우저 기준

조카 집은 스마트TV 내장 브라우저(삼성 타이젠 / LG webOS)로 접속한다. 크롬캐스트나 노트북 HDMI 없음.

- **Three.js 사용 금지.** WebGL이 지원돼도 프레임이 안 나온다. 2D Canvas + 원근 스프라이트로 2.5D를 표현한다.
- **30fps를 목표로 설계.** 60fps 전제로 만들면 실기에서 게임 속도가 절반이 된다. 모든 움직임은 `deltaTime` 기반.
- **캔버스 720p로 그리고 CSS로 확대.** 1080p 실시간 채우기는 부담.
- **그림자·블러·대량 파티클 금지.** 특히 블록깨기 코인 연출. 개수 제한 + 미리 그린 애니메이션 스프라이트로 대체.
- **오버스캔 대비 안전영역 5%.** 구형 TV는 가장자리가 잘린다.
- 구형 webOS 대응 필요 시 ES5 문법 유지 검토.

성능 실측은 `tv-bench.html`로 조카 집 TV에서 진행. 결과에 따라 위 수위를 재조정한다.

---

## 3. 그래픽 / 캐릭터

- **2.5D** (픽셀아트 아님). 쿼터뷰 스프라이트 + 원근 스케일링.
- 주인공: **M 대신 L 마크가 있는 빨간 모자 + 멜빵** 캐릭터. 직접 제작.
- 그 외 마리오·루이지·쿠파·키노피오·공주는 원작 캐릭터 사용.
- **집에서만 사용하는 전제.** 외부 공개·배포 계획 없음. 공개하게 되면 캐릭터를 전부 교체해야 한다.

---

## 4. 동작 판정 명세

폰을 가슴에 쥐고 플레이한다. 판정 모듈은 **반드시 공통 라이브러리로 분리**할 것.
게임별로 흩어놓으면 임계값 튜닝 때 다섯 군데를 고치게 된다.

### 감지하는 동작 4종

| 동작 | 사용 센서 | 판정 방식 |
|---|---|---|
| 점프 | 가속도 세로축 | 스파이크 + **직전 자유낙하 구간** |
| 쳐올리기 | 가속도 세로축 | 스파이크, 자유낙하 구간 **없음** |
| 스쿼트 | 가속도 세로축 (적분한 속도) | 아래로 충분히 내려갔다 다시 올라오는 왕복 1사이클 |
| 좌우 기울기 | 자이로 roll | 각도 → 레인 위치 매핑 |

### 점프와 쳐올리기 구분 (중요)

둘 다 세로 가속도 스파이크라 임계값만으로는 서로 오인식된다. 결정적 차이는 체공 구간.

```
스파이크 직전 0.3초 창에서
  합가속도가 3m/s² 아래로 떨어진 적이 있으면  → 점프 (자유낙하)
  없으면                                    → 쳐올리기 (발이 바닥에 붙어 있음)
```

미니게임 하나당 동작 하나만 쓰므로 당장은 불필요하지만, 판정기는 처음부터 분리해서 만든다.

### 스쿼트

세로 가속도를 누설 적분한 **세로 속도**의 왕복을 본다. 아래로 임계를 넘었다가(그 순간 가속도도
동작이라 부를 만큼 커야 한다) 위로 임계를 넘으면 1회. 발화는 일어나기 시작하는 시점.

> 처음엔 자이로 pitch로 판정했다 (2026-09-17 이전). "가슴에 쥐면 앉을 때 몸이 앞으로 숙여진다"는
> 전제였는데, 실기에서 폰을 같은 높이에서 카메라 쪽만 젖혀도 스쿼트로 잡혔다. 각도는 높이를
> 말해주지 않는다. 각도는 이제 스쿼트에 쓰지 않는다.

세로축은 보정 때 중력이 아니라 **지금 폰이 보는 중력 방향**(저역 통과 추정)을 쓴다.
보정 때 각도에 고정하면 폰을 젖힌 채 앉았다 일어날 때 cos 손실이 상수 편향으로 끼어 속도가 한쪽으로 흐른다.

### 스쿼트 · 쳐올리기 · 점프는 한 신호를 나눠 갖는다 (중요)

셋 다 세로축이다. 스쿼트 = 아래로 먼저 갔다 올라옴. 쳐올리기 = 위로 먼저 감. 점프 = 웅크렸다 올라간 뒤 발이 뜸.
따로 판정하면 서로 샌다 — 힘차게 일어서면 쳐올리기 스파이크가 나고, 쳐올린 폰이 가슴으로 돌아오면
세로 왕복이 생기고, 점프의 도약은 스쿼트의 일어남과 같다. 그래서 **먼저 잡힌 방향이 이긴다.**

- 스쿼트 사이클이 진행 중이거나 스쿼트 직후 `SQUAT_PUNCH_GUARD_MS` 안이면 쳐올리기를 내지 않는다.
- 쳐올리기 직후 같은 시간 안에는 스쿼트의 "내려감"에 들어가지 않는다.
- 스쿼트는 일어나기 시작하고 `SQUAT_CONFIRM_MS` 뒤에 낸다. 그 사이 체공이 시작되면 점프였으므로 버린다.
- 이 배제는 게임이 동작 하나만 켰을 때도 작동한다 (잡힌 시각은 켜짐과 무관하게 기록).

### 좌우 기울기

- **기울기로 위치를 지정**하는 방식(절대 위치). 스텝 스파이크 감지 방식은 오인식이 잦아 채택하지 않음.
- 3레인 기준: 왼쪽 임계 초과 = 1번, 중앙 = 2번, 오른쪽 임계 초과 = 3번.
- **임계값은 10~12도로 낮게.** 그러면 아이는 자연스럽게 몸을 옆으로 옮기게 되고, 판정은 안정적이다.

### 센서 보정

게임 시작 전 "폰을 가슴에 대고 똑바로 서세요" 3초 캘리브레이션 필수.
없으면 폰을 쥔 각도에 따라 기준이 매번 달라져 판정이 무너진다. **미니게임이 바뀔 때마다 재보정**.

---

## 5. 미니게임 5종

동작·요구능력·체력부하가 전부 겹치지 않도록 설계됨.

| 게임 | 동작 | 요구 능력 | 체력 | 구현 난이도 |
|---|---|---|---|---|
| 줄넘기 | 점프 | 리듬·타이밍 | 높음 | 매우 낮음 |
| 로프 클라이밍 | 스쿼트 | 반복 지구력 | 높음 | 낮음 |
| 해머 피하기 | 좌우 스텝 | 순간 판단 | 중간 | 중간 |
| 블록깨기 | 조준 + 쳐올리기 | 정확도 | 낮음 | 중간 |
| 레이싱 | 좌우 기울기(손목) | 지속 미세 조작 | 낮음 | 매우 높음 |

### 5-1. 줄넘기

- 캐릭터 제자리 고정, 줄만 회전. 카메라 이동·스크롤 없음. 배경은 정지 화면 한 장.
- 5세에게 타이밍은 어렵다. **줄 속도를 느리게 시작해 10개마다 조금씩 가속.**

### 5-2. 로프 클라이밍

- 세로 스크롤. 4레인(주인공 + 마리오·공주·쿠파)이 나란히 올라가는 경주.
- 스쿼트 1회 = 한 칸 상승. "앉았다 일어나면 올라간다"는 인과가 화면과 일치.
- **로프 높이는 스쿼트 25회 정도, 한 판 60~90초.** 5세가 쉬지 않고 가능한 스쿼트는 20~30회.
- 연출: 리듬 일정하면 콤보 가속 / 로프에 코인 배치 / 쿠파가 밥밥탄 투하(감점 대신 0.5초 정지) / 정상에 깃발.

### 5-3. 해머 피하기

- 해머브라더스가 위에서 해머 투하, 3레인을 오가며 회피. 마리오1의 그 장면.
- **레인 3개.** 5개는 너무 많고 2개는 금방 질린다.
- **예고선 필수.** 낙하 0.8초 전 해당 레인 바닥이 빨갛게 깜빡. 없으면 반사신경 싸움이 된다.
- 속도는 10초마다 계단식 상승. 갑작스러운 가속 금지.
- 후반에 **두 레인 동시 투하** → 나머지 한 곳으로 뛰어야 함. 이 게임의 하이라이트.
  **세 레인 전부 막는 패턴은 절대 금지.**
- 맞아도 게임오버 없이 0.5초 스턴 + 코인 소량 손실. 점수는 버틴 시간.
- **코인 레인**을 섞어 일부러 특정 레인으로 갈 이유를 만든다. 움직임이 훨씬 활발해진다.

### 5-4. 블록깨기

- 3×3 격자. 폰을 좌우로 기울여 조준 → 위로 툭 쳐올려서 블록 파괴.
- **로직 20%, 연출 80%.** 대충 만들면 존재 이유가 사라진다. 단, TV 성능 한계 내에서.
- 블록 종류: 코인(기본) / 버섯(5초간 2배) / 스타(3초간 조준 없이 전부 파괴) / 빈 블록(꽝, 소리만) / 밥밥탄(시간 1초 차감)
- **감점 최소화.** 5세는 마이너스가 뜨는 순간 흥미를 잃는다.

### 5-5. 레이싱

- 나머지 넷을 합친 것보다 작업량이 크다. **가장 마지막에 구현.**
- 렌더링은 **아웃런식 스캔라인 유사 3D** (가로줄 단위 도로 렌더링). 연산이 가볍고 2.5D 질감이 난다.
- 필요 요소: 도로 곡률, 카메라, 코스 데이터, 충돌, 랩 카운트, NPC 주행 라인, 스프라이트 깊이 정렬.

---

## 6. NPC 설계

**"AI"는 게임 용어로서의 NPC를 뜻한다. 머신러닝·LLM 사용 없음.** if문 몇 줄이 전부.

```js
npc.speed = player.speed * 0.85;
if (remaining < 0.2) npc.speed *= 1.15;   // 막판 추격
```

- **쿠파를 페이스메이커로.** 조카보다 살짝 느리되 계속 따라붙게. 압도적으로 이기면 질리고, 압도적으로 지면 폰을 던진다.
- **러버밴딩 필수.** 플레이어의 실시간 속도(초당 스쿼트 수 등)를 재서 그 80~90%로 추격.
- **마지막 20% 구간에서 역전 연출.** 쿠파가 살짝 앞서다 막판에 따라잡히는 게 가장 짜릿하다.
- **꼴등 없음.** 완주하면 다 같이 골인 연출.
- 배수 값(0.85 등)은 코드 문제가 아니라 **플레이테스트로 찾아야 하는 숫자.** 조카 표정을 보고 정한다.

---

## 7. 화면 목록

### TV (`/tv`)

1. **대기** — 4자리 방 코드 크게(화면 1/3 크기, 소파에서 읽혀야 함) + 접속 플레이어 목록. 접속 시 캐릭터 등장 연출
2. **캐릭터 선택** — L모자 / 마리오 / 루이지 / 공주 / 쿠파 / 키노피오
3. **미니게임 선택 + 룰렛** (아래 별도 항목)
4. **동작 시연** — 글자 대신 캐릭터가 해당 동작을 3초간 반복. 텍스트 설명은 어른용으로 작게
5. **카운트다운** — 3, 2, 1, GO
6. **게임 플레이** — 미니게임 5종
7. **결과** — 시상대, 폭죽, 점수 획득 애니메이션
8. **총점 / 최종 우승**

### 폰 (`/play`)

1. **코드 입력** — 아래 별도 항목
2. **캐릭터 선택**
3. **준비 완료**
4. **컨트롤러** — **색 하나와 큰 아이콘 하나만.** 점수·캐릭터 그리지 않음
5. **결과 대기**

폰 화면 5개는 **단일 페이지 + 상태 머신**으로 구현. TV가 WebSocket으로 현재 단계를 알려주면 폰이 UI를 바꾼다. 라우팅 불필요.

### 컨트롤러 화면 필수 요건

- **동작 인식 피드백** — 인식되면 화면 전체 번쩍 + 진동. 없으면 인식 여부를 몰라 답답해한다
- **Wake Lock API** — 없으면 게임 도중 폰이 잠긴다
- 연결 끊김 시 크게 표시
- 화면 방향 고정

### 방 코드 입력

- **QR 사용 안 함.** 4자리 숫자만.
- 숫자 키패드를 화면 가득. 버튼 하나 최소 80px.
- 4자리 입력 시 자동 접속. 확인 버튼 없음.
- **코드 생성에 2,3,4,5,8,9만 사용.** 0과 6, 1과 7은 5세에게 비슷하게 보인다.
- 틀리면 흔들림 애니메이션 + 자동 초기화.
- 어른이 대신 입력하고 넘겨주는 흐름도 자연스럽게 지원.

### 미니게임 선택 / 룰렛 (둘 다 지원)

- 미니게임 5개가 카드로 나열 + 아래에 룰렛 버튼.
- 룰렛: 커서가 카드 사이를 빠르게 돌다 감속하며 정지. **폰을 흔들어 돌리게 하면 더 좋다.**
- 카드 직접 선택 시 즉시 시작.
- **직전 게임은 룰렛에서 제외.**
- **체력 가중치**: 직전이 하체 부하 높음(줄넘기·로프)이었으면 다음 룰렛에서 그 둘을 제외. 겉으로는 랜덤이지만 힘든 게임이 연속으로 안 나온다.
- 기본은 룰렛, 선택은 보조. 어른이 "이제 앉아서 하는 거 하자"고 개입할 여지를 남긴다.

---

## 8. 통신 프로토콜 (초안)

```
WS  /ws/{code}?role=tv|play

TV → 서버
  { "t": "create" }                      방 생성, 코드 발급
  { "t": "phase", "v": "select|play|result", "game": "rope" }
  { "t": "state", "v": {...} }           게임 상태 브로드캐스트 (필요 시)

폰 → 서버
  { "t": "join", "name": "...", "char": "luigi" }
  { "t": "motion", "a": "jump|squat|punch", "p": 0.82 }   동작 이벤트 + 세기
  { "t": "tilt", "v": -0.42 }            연속값 (-1 ~ 1), 레이싱·해머용
  { "t": "calib", "v": {...} }           보정 기준값
```

- **동작 이벤트는 폰에서 판정해서 결과만 보낸다.** 원시 센서값 60Hz를 그대로 흘리지 않는다.
- 예외: `tilt`는 연속값이 필요하므로 20~30Hz로 스로틀링해서 전송.
- 서버는 방 단위로 메시지를 그대로 릴레이한다. 게임 로직 없음.

---

## 9. 개발 순서

1. **`tv-bench.html`로 조카 집 TV 성능 실측** (벤치는 만들어 뒀다. 실측은 조카 집 방문 때)
2. NAS FastAPI 릴레이 + HTTPS(DDNS + Let's Encrypt) 구성 (서버 코드 완료. 배포 절차는 `docs/deploy.md`)
3. **줄넘기 한 판** — 대기 → 카운트다운 → 플레이 → 결과. 통신 지연·센서 권한·HTTPS·TV 연결이 여기서 전부 검증된다 (게임 완성. 검증은 방문 때)
4. 로프 클라이밍 (러버밴딩 튜닝) — 완성. 배수는 조카 표정 보고 정한다
5. 해머 피하기 — 완성. 미니게임 선택/룰렛도 여기서 붙었다
6. 블록깨기 — 완성
7. 레이싱 ← 현재 여기

3~5까지만 끝나도 놀 수 있는 게임이 된다. **조카에게 한 번 보여주고 반응을 본 뒤 나머지를 만든다.**
실제로 놀려보면 예상 못 한 문제(폰 쥐는 법, 뛰는 범위, 흥미를 잃는 지점)가 반드시 나온다.

### 미니게임 순서 배치

체력 부하 기준 **낮음 → 높음 → 낮음 → 높음 → 중간**.
줄넘기와 로프를 연달아 붙이지 말 것. 둘 다 하체 유산소라 연속이면 지친다.

---

## 10. 5세 대상 설계 원칙

- **한 판 90초.** 길면 폰을 내려놓고 다른 데로 간다
- **감점보다 정지·지연으로 페널티를 준다**
- **꼴등·게임오버 없음**
- **글자 최소화.** 아이콘·색·소리·진동으로 전달
- **효과음과 진동이 중요하다.** 동작 중에는 폰 화면을 볼 수 없다
- 경쟁과 점수는 이 나이대에 **재미의 핵심**이다. 다만 반드시 이길 수 있어야 한다

---

## 11. 코드 규칙

- 주석과 docstring은 **한국어**
- 프론트엔드는 **바닐라 JS**, SPA 프레임워크 사용 안 함
- 파일 버전은 덮어쓰지 말고 숫자 접미사(`_1`, `_2`)
- 동작 판정은 **공통 모듈 하나**로 유지
- 게임 로직은 입력 소스에 의존하지 않게 작성 (이벤트를 받는 형태)

---

## 12. 미결정 사항

### 정해진 것

- [x] **점수 합산 구조 → 마리오파티식 보드.** 24칸 원형 보드, 판 결과를 칸 수로 환산해 이동한다.
      단순 합산을 안 쓴 이유: 게임마다 점수 단위가 다르다(줄넘기 개수 / 로프 칸 / 해머 초 / 블록 점수).
      그냥 더하면 해머 90점과 줄넘기 12개가 같은 저울에 올라간다. 보드가 저울을 통일한다.
      **뒤로 가는 칸은 없다.** 쿠파 칸은 멈출 뿐이고, 별 칸은 지나가도 하나를 준다. 구현은 `js/board.js`.
- [x] **폰 2대 이상 → 2~4명 지원.** 서버가 p1~p4를 배정하고 게임마다 자리를 나눈다.
      로프는 사람이 레인을 채우고 남는 자리를 NPC가 메운다. 해머·블록깨기는 같은 화면에서
      캐릭터 색으로 구분한다. 다섯 번째 폰은 서버가 "방이 가득 찼어요"로 거절한다.
- [x] **사운드 에셋 확보 방법 → 당분간 확보하지 않는다. WebAudio로 합성한다** (`js/sfx.js`).
      효과음 16종과 화면별 BGM 루프가 오실레이터로 만들어진다. 에셋을 기다리면 피드백이 통째로
      비고, 피드백이 없으면 조카는 인식 여부를 모른다. 실제 에셋이 생기면 `play()` 안쪽만 바꾼다.
      TV 브라우저가 자동재생 정책으로 소리를 못 내면 **폰이 대신 짧은 소리를 낸다** — 폰은 코드를
      누르며 이미 사용자 제스처를 만들었고, TV는 입력을 안 받아 만들 수가 없다.

### 아직 남은 것 (전부 조카 집에 가야 풀린다)

- [ ] 조카 집 TV 모델·연식 확인 → 렌더링 수위 확정
- [ ] `tv-bench.html` 측정 결과
- [ ] 도메인 (리모컨 입력을 고려해 최대한 짧게, 루트에서 TV 화면이 열리도록)
