# Claude Code 개인 설정. 공용 설정은 .claude/settings.json 에 둔다
.claude/settings.local.json

# playwright MCP 실행 부산물 (스냅샷·콘솔 로그)
.playwright-mcp/

# 검증용 임시 파일. 실제 에셋은 assets/ 아래에 둔다
*.tmp

# 루트에 떨어진 스크린샷. 확인하다 남긴 것이지 저장소에 있을 물건이 아니다.
# 진짜 그림은 assets/ 아래에 있으므로 루트만 막는다.
/*.png

# 의존성 (지금은 없지만 나중에 도구가 붙을 경우)
node_modules/

# OS
Thumbs.db
desktop.ini
.DS_Store

# Netlify 배포용 산출물. netlify-build.sh가 만든다
dist/

# Python 캐시
__pycache__/
*.pyc
